# T1 工作区安全整理报告

日期：2026-08-05

## 敏感脚本整理

已创建目录 `D:\xm\90_temp_work\_private\ssh_helpers\`，并将下列三个指定脚本从工作区根目录移入该目录：

- `ssh_connect.py`
- `ssh_interactive.py`
- `status_check.py`

未删除任何文件，未触碰 `_private` 中的其他内容。本文档不包含任何密码、密钥或其他凭据明文。

## XeLaTeX 技能改动审查

审查对象：`.opencode/skills/xelatex-compile/SKILL.md` 的未提交改动。

### 结论

建议提交。改动具备明确的实用价值：将原本通用但较简略的编译说明，扩展为贴合当前工作区项目布局的操作手册。

### 优点

- 规范了两遍 XeLaTeX 编译、UTF-8 代码页与独立的 pass1/pass2 日志保存方式，有利于中文文档与故障定位。
- 增加项目关键词到构建流程的决策表，可减少选择错误脚本或错误入口的情况。
- 补全 SG、内置编译、PDF 重建等项目类型，并给出了具体输出目录和典型产物。
- 增加成功判定、错误定位和常见故障的处置说明，覆盖了实际维护中最常见的编译问题。
- 修正了原文件末尾缺失换行的问题。

### 提交前建议

- 文档中的绝对路径和项目清单与当前工作区状态强耦合；提交前应确认列出的目录、脚本和输出目录仍存在且名称准确。
- “必须两遍编译”应理解为默认流程；若第二遍仍提示交叉引用需要重跑，文档已有说明要求继续编译，表达合理。
- “删除 `.aux`/`.toc` 等缓存文件”属于排障步骤；执行时应限定在目标项目目录，避免误清理不相关文件。

整体而言，内容准确、可操作性明显提升，值得作为工作区技能文档提交。
